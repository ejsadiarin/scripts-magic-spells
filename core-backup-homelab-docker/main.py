"""
This script should:
    - Raw local backup to /mnt/Backups/core/raw (HDD) via rsync
    - Raw remote backup to backblaze b2 via restic
    - Scheduled to run every week or Run manually
"""


def main():
    print("tite")


if __name__ == "__main__":
    main()
